#include <stdio.h>
#include "addi.h" // quote user's header file

int main()
{
    printf("Hello VSCode!\n");
    int a = 3, b = 2;
    int c = addi(a, b);
    printf("%d", a);
    if (a>0)
    {
        printf("%d", a);
    }

    return 0;
}